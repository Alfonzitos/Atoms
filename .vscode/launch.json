{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Atoms",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build atoms",
            "stopAtEntry": false,
            "MIMode": "gdb"
        }
    ]
}